image: node:6

pages:
  stage: deploy
  script:
    - npm install -g grunt-cli bower
    - npm install
    - bower install --allow-root 
    - grunt build
    - mkdir public
    - cp -a dist/* public/
  artifacts:
    paths:
    - public
  only:
  - master
